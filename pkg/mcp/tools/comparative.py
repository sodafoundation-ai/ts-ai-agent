# top_n_instances	Return top N instances by a metric	metric_name, labels, n, window	List of {instance, value}
# bottom_n_instances	Bottom N instances	Same as above	List of {instance, value}
# compare_metrics	Difference between two metrics	metric_a, metric_b, labels, window	delta_value
# percent_change	Compute % change over time	metric_name, window_start, window_end	percent_change